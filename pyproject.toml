[project]
name = "thompson-sampling"
version = "0.1.0"
description = "Thompson sampling for Bayesian multi-armed bandit."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "tqdm>=4.67.1",
]


[tool.ruff]
line-length = 100
